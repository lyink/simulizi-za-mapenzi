rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for development - anyone can read and write
    // TODO: Add authentication and restrict write access for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
